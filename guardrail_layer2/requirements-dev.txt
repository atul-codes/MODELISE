# Install alongside requirements.txt only if you want to run the test
# suite: pip install -r requirements.txt -r requirements-dev.txt
pytest==9.1.1
pytest-asyncio==1.4.0
respx==0.23.1       # mocks the Ollama HTTP calls so tests don't need a live model
fpdf2==2.8.2        # only needed if you regenerate tests/fixtures/sample_compliance.pdf
